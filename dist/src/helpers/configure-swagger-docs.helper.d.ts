import { INestApplication } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
export declare function configureSwaggerDocs(app: INestApplication, configService: ConfigService): void;
