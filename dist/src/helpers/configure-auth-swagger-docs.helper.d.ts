import { INestApplication } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
export declare function configureAuthSwaggerDocs(app: INestApplication, configService: ConfigService): void;
